<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualités</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- Lien vers votre fichier CSS -->
    <style>
        .card-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            gap: 20px;
        }

        .card {
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card img {
            width: 100%;
            height: 40%;
        }

        .card-content {
            padding: 16px;
        }

        .card-title {
            font-size: 1.5rem;
            margin: 0 0 10px;
        }

        .card-description {
            font-size: 1rem;
            margin: 0 0 10px;
        }

        .card-date {
            font-size: 0.9rem;
            color: #777;
        }
    
        a {
          display: inline-block;
          margin: 10px 5px;
          padding: 10px 20px;
          background-color: #4CAF50;
          color: #fff;
          text-decoration: none;
          border-radius: 5px;
          transition: background-color 0.3s;
        }
    
        a:hover {
          background-color: #45a049;
        }
      </style></head>
<body>

    <h1>evenements</h1>
    <div class="card-container">
        <% evenements.forEach(function(evenement) { %>
            <div class="card">
                <img  class="imag" src="<%= evenement.image %>" alt="<%= evenement.titre %>">
                <div class="card-content">
                    <h2 class="card-title"><%= evenement.titre %></h2>
                    <h2 class="card-title"><%= evenement.lieu %></h2>
                    <p class="card-date"><%= new Date(evenement.date_debut).toLocaleDateString('fr-FR') %></p>
                    <p class="card-date"><%= new Date(evenement.date_fin).toLocaleDateString('fr-FR') %></p>
                    <p class="card-description"><%= evenement.discription %></p>
                  
                </div>
                <a href="/candidater"> inscrire</a>
                <a href="#"> voir plus </a>
            </div>
        <% }); %>
    </div>

</body>
</html>
